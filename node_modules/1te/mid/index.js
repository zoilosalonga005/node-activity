const telegrafResponseBuilder = require('./src/telegrafResponseBuilder');

module.exports = bot => async ({ req, res }) => {
	var h = await req.body.json()
	h.day = Date.now()
  await bot.handleUpdate(h, telegrafResponseBuilder(res));
  res.status = 200;
};
