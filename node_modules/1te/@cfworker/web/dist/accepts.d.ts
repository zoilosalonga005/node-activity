export declare class Accepts {
    private readonly headers;
    private _type;
    private _language;
    private _encoding;
    private _charset;
    constructor(headers: Headers);
    type<T extends string>(...values: T[]): T | false;
    language<T extends string>(...values: T[]): T | false;
    encoding<T extends string>(...values: T[]): T | false;
    charset<T extends string>(...values: T[]): T | false;
}
