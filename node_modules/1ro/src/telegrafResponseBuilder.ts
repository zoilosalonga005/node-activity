import { ResponseBuilder } from './response-builder.js';

class TelegrafResponseBuilder extends ResponseBuilder {
  header: Headers;
  set: (name: string, value: string) => void;
}

export function getKoaLikeResponse(res: ResponseBuilder): TelegrafResponseBuilder;

